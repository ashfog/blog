---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="关于 AshFog" description="AshFog 的编辑原则、证据标准与自动发布方式。">
  <section class="about page-shell">
    <header>
      <span class="eyebrow">About AshFog</span>
      <h1>不是更多新闻。<br /><em>是更少噪音。</em></h1>
      <p>AshFog 每天整理 AI、开源、开发工具和基础设施的重要变化，让每一条事实都能回到来源，让每一个判断都看得见证据边界。</p>
    </header>

    <div class="principles">
      <article>
        <span>01</span>
        <h2>来源优先</h2>
        <p>官方公告、仓库 Release、论文和维护者说明优先。媒体与社区用于发现，而不是替代原始证据。</p>
      </article>
      <article>
        <span>02</span>
        <h2>质量优先</h2>
        <p>没有足够高质量内容时，日报会明确说明数量不足，不用旧闻、融资新闻或营销发布凑满配额。</p>
      </article>
      <article>
        <span>03</span>
        <h2>社区分级</h2>
        <p>单次测试、多用户报告、维护者确认各自标注。热度、票数和转发量永远不会被写成事实证明。</p>
      </article>
      <article>
        <span>04</span>
        <h2>机器校验</h2>
        <p>发布前检查严格 JSON、日期窗口、重复事件、来源身份、链接状态、栏目比例和最近七期重复。</p>
      </article>
    </div>

    <section class="manifesto">
      <p>“Not everything worth seeing is visible.”</p>
      <h2>灰雾不是遮挡，<br />而是提醒我们：<br /><strong>真正重要的东西，需要被辨认。</strong></h2>
    </section>
  </section>
</BaseLayout>

<style>
  .about {
    padding-top: 130px;
  }

  header {
    display: grid;
    min-height: 610px;
    grid-template-columns: 2fr 1fr;
    gap: 60px;
    align-items: end;
    padding-bottom: 80px;
  }

  .eyebrow {
    grid-column: 1 / -1;
    align-self: start;
  }

  h1 {
    margin: 0;
    font-size: clamp(62px, 9vw, 120px);
    font-weight: 510;
    letter-spacing: -0.075em;
    line-height: .88;
  }

  h1 em {
    color: var(--accent);
    font-family: var(--font-serif);
    font-weight: 430;
  }

  header p {
    margin: 0;
    color: var(--muted);
    font-size: 17px;
  }

  .principles {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border-top: 1px solid var(--line);
    border-left: 1px solid var(--line);
  }

  .principles article {
    min-height: 340px;
    padding: 26px;
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    background: var(--surface);
  }

  article span {
    color: var(--accent);
    font-size: 11px;
  }

  article h2 {
    margin: 120px 0 14px;
    font-size: 26px;
    font-weight: 540;
    letter-spacing: -.04em;
  }

  article p {
    color: var(--muted);
    font-size: 13px;
  }

  .manifesto {
    min-height: 700px;
    padding-top: 140px;
  }

  .manifesto > p {
    color: var(--faint);
    font-family: var(--font-serif);
  }

  .manifesto h2 {
    margin: 35px 0;
    font-family: var(--font-serif);
    font-size: clamp(42px, 6.5vw, 86px);
    font-weight: 430;
    letter-spacing: -.05em;
    line-height: 1.08;
  }

  .manifesto strong {
    color: var(--accent);
    font-weight: inherit;
  }

  @media (max-width: 800px) {
    header {
      grid-template-columns: 1fr;
    }

    .principles {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 520px) {
    .principles {
      grid-template-columns: 1fr;
    }
  }
</style>
